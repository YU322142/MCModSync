# 更新日志

[English](CHANGELOG.md)

## 2.0.0

- CurseForge 发布解析会把 MCIMirror API 返回的 ForgeCDN 路径转换为镜像文件地址，并对每个候选执行大小与 SHA-256 复核；简体中文系统优先镜像并回退官方地址，其他语言只使用官方地址。

### 主要更新

- MCModSync 更名为 MCSync，同时保留旧版升级入口。
- 新增 `mods-v5.json`、发布序号防降级和同序号分叉拒绝。
- 新增启动早期事务同步，包含下载校验、备份、提交和回滚。
- 新增首次启动和新增可选内容时的游戏内必须/推荐选择界面。
- 新增 2.0 发布 GUI，集中管理 Mods、受管文件、配置键级 OTA、可选包、服务器列表、验证和导出。
- Modrinth 按内容哈希匹配；CurseForge fingerprint 仅用于定位候选，发布器必须再下载并通过大小与 SHA-256 复核，否则放弃该平台来源。
- 唯一 modId 的替换文件会被识别为升级，继承发布者编辑的设置，并按新 JAR 内容重新匹配来源。
- 同一 modId 出现多个 JAR 时全部标记为冲突并阻止导出，不自动选择某个版本。
- 只有 `mods/` 直属 JAR 会查询模组平台，其他受管文件使用普通下载来源。
- 支持直链、Modrinth、CurseForge 和可配置镜像，并统一校验大小与 SHA-256。
- 提供受支持的 1.6.x、1.7 和 1.9.x 客户端升级材料。
- 发布器可直接选择上一版完整发布输出目录，与当前待发布客户端目录按大小和 SHA-256 对比；无需人工选择旧 `mods-v5.json`，未变化文件复用不可变 URL，当前版本目录只导出新增或变化的升级内容。
- 导出生成机器可读增量计划和内容等价的中英文上传/替换指南。
- 发布阶段把 Modrinth 固定版本解析成哈希匹配的文件 URL；客户端本地文件正常时不再访问模组站元数据。
- 修复 NeoForge 大型整合包中 MCSync 进度条被其他加载条挤出可视区域的问题；早期窗口现在将 MCSync 置顶，并使用该窗口能够可靠显示的 ASCII 状态标签。
- 明确区分 Minecraft 窗口内的下载/缓存/哈希校验阶段与退出后的隐藏原子提交阶段，不再把隐藏提交助手误报成不可用的图形窗口。
- 隐藏提交开始前在 Minecraft 早期窗口显示不可跳过的退出倒计时，明确提醒玩家等待原子提交完成后再重新启动。
- Minecraft 退出前根据实际待写文件数量和字节量显示隐藏提交预计耗时；大量小文件或较慢磁盘会明确提示可能需要更久。
- 修复 schema-v5 新发布序号会把完整终态清单中的全部文件重新暂存、备份和写回的问题；完整清单仍会逐项校验并进入所有权账本，但事务只提交新增、变化、删除和配置变更。
- 上一版基线现在可直接选择完整发布输出、单个 `manifest-v5.json`/`mods-v5.json` 或 ZIP 升级包。升级包只需携带完整终态索引，不必重复携带旧文件；缺少完整索引时发布器会阻止导出。

## 1.9.6

### 主要更新

- MCModSync 1.9.x 系列最后一个公开维护版本。
- 修复 NeoForge/Linux 辅助进程的桌面同步窗口。
- 默认最多使用 8 个并行下载线程，并且只重试失败任务。
- 保留已经成功校验的下载，并保存本地 `mods-v4.txt` 清单副本。
- 继续编辑旧清单时生成新的当前时间清单序号。
- 提供中英文界面与日志，以及 Fabric/NeoForge 1.21.1 元数据。

更早的 1.9.x 开发记录保留在 Git 历史中。
