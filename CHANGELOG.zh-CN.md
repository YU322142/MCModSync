# 更新日志

[English](CHANGELOG.md)

## 2.0.0

- 云端托管从时间戳版本目录改为 SHA-256 内容寻址对象库：`objects/sha256/<前两位>/<完整哈希>`。发布序号现在只承担防降级，不再参与文件路径；完整历史清单位于 `manifests/<releaseId>.json`，稳定清单最后替换。
- 新增重新全量发布模式：可以沿用同哈希的平台验证证据，但不会复用或省略任何本地托管文件，因此既能建立干净全量基线，也不会重复下载已验证的 Modrinth/CurseForge 文件。
- GUI 中旧版升级网关改为默认关闭，并新增全量云端发布命令。MCSync 自身仍作为必需受管 Mod 进入清单，且发布工具、清单和对象库三方哈希必须一致。
- 修复 Iris、Indigo 与 PacketFixer 启动时重写属性文件时间注释而触发反复重启的问题：Iris/Indigo 改为首次安装策略，PacketFixer 不再进行整文件覆盖。

- 修复发布器新增同步范围后“扫描安全内容目录”不追加文件的问题；扫描现在以当前范围表为准，并纳入 `config/`、`defaultconfigs/`、`configureddefaults/`。账号/会话、凭据、备份、临时文件、运行态缓存及含敏感键的配置会按路径和内容黑名单跳过，并在界面列出原因；精确配置修复继续使用键级 OTA。
- 新增配置自动分流：玩法一致性配置按 `additive` 管理；客户端画面、音量、按键和 UI 配置自动设为文件级 `first-install`；同时包含玩法键与个人键的混合文件不会整文件发布，而是明确要求使用配置项级 OTA。
- 修复在同步范围单元格中刚输入自定义目录后立即扫描仍读取旧值的问题。FancyMenu 的正式布局、素材和主题从 `config/fancymenu/` 识别为整合包 UI 内容；`fancymenu_data/` 中的上次世界、Buddy 玩家进度、浏览器状态和临时缓存会显示原因并排除。
- 修复简体中文 Windows 被启动器 `-Duser.language=en` 错误路由成英文线路的问题；下载线路现在优先读取操作系统当前用户区域，仅在无法读取时回退 JVM/环境区域。
- 通过大小与 SHA-256 复核的 CurseForge ForgeCDN 文件地址，即使其元数据来自中国镜像 API，也会规范化为官方全球端点；简体中文仍优先镜像，其他语言则始终拥有可用的官方回退。
- 明确自更新边界：客户端只能安装稳定 `mods-v5.json` 当前发布的 MCSync 哈希，尚未发布的本地构建无法被自动发现；发布清单中任一必需文件不可解析时，整笔原子事务（包括 MCSync 自更新）都会闭锁。

- 发布工作台新增真实发布进度条，逐阶段显示哈希与平台核验、托管文件复制、清单写入和云端发布包整理进度；耗时文件校验不再阻塞 GUI。
- 选择上一版发布输出后，平台 Mod 会按 SHA-256、大小和固定平台坐标复用已验证下载证据；内容与平台坐标均未变化时不再重复请求平台 API 或下载整个 Mod。
- 平台文件一旦验证成功便立即将小型验证证据原子写入客户端根目录的 `.modsync/publisher-platform-verifications-v1.json`；即使本次发布稍后失败，重试也不会重复下载已经验证过的 Mod。

- CurseForge 发布解析会把 MCIMirror API 返回的 ForgeCDN 路径转换为镜像文件地址，并对每个候选执行大小与 SHA-256 复核；简体中文系统优先镜像并回退官方地址，其他语言只使用官方地址。
- CurseForge 的专用 `download-url` 接口临时故障时，会回退到同一固定 `projectId/fileId` 的完整文件元数据读取下载地址，随后照常执行实际文件 SHA-256 校验。

### 主要更新

- MCModSync 更名为 MCSync，同时保留旧版升级入口。
- 新增 `mods-v5.json`、发布序号防降级和同序号分叉拒绝。
- 新增启动早期事务同步，包含下载校验、备份、提交和回滚。
- 新增首次启动和新增可选内容时的游戏内必须/推荐选择界面。
- 新增 2.0 发布 GUI，集中管理 Mods、受管文件、配置键级 OTA、可选包、服务器列表、验证和导出。
- Modrinth 按内容哈希匹配；CurseForge fingerprint 仅用于定位候选，发布器必须再下载并通过大小与 SHA-256 复核，否则放弃该平台来源。
- 唯一 modId 的替换文件会被识别为升级，继承发布者编辑的设置，并按新 JAR 内容重新匹配来源。
- 同一 modId 出现多个 JAR 时全部标记为冲突并阻止导出，不自动选择某个版本。
- 只有 `mods/` 直属 JAR 会查询模组平台，其他受管文件使用普通下载来源。
- 支持直链、Modrinth、CurseForge 和可配置镜像，并统一校验大小与 SHA-256。
- 提供受支持的 1.6.x、1.7 和 1.9.x 客户端升级材料。
- 发布器可直接选择上一版完整发布输出目录，与当前待发布客户端目录按大小和 SHA-256 对比；无需人工选择旧 `mods-v5.json`，未变化文件复用不可变 URL，当前版本目录只导出新增或变化的升级内容。
- 导出生成机器可读增量计划和内容等价的中英文上传/替换指南。
- 发布阶段把 Modrinth 固定版本解析成哈希匹配的文件 URL；客户端本地文件正常时不再访问模组站元数据。
- 修复 NeoForge 大型整合包中 MCSync 进度条被其他加载条挤出可视区域的问题；早期窗口现在将 MCSync 置顶，并使用该窗口能够可靠显示的 ASCII 状态标签。
- 明确区分 Minecraft 窗口内的下载/缓存/哈希校验阶段与退出后的隐藏原子提交阶段，不再把隐藏提交助手误报成不可用的图形窗口。
- 隐藏提交开始前在 Minecraft 早期窗口显示不可跳过的退出倒计时，明确提醒玩家等待原子提交完成后再重新启动。
- Minecraft 退出前根据实际待写文件数量和字节量显示隐藏提交预计耗时；大量小文件或较慢磁盘会明确提示可能需要更久。
- 修复 schema-v5 新发布序号会把完整终态清单中的全部文件重新暂存、备份和写回的问题；完整清单仍会逐项校验并进入所有权账本，但事务只提交新增、变化、删除和配置变更。
- 上一版基线现在可直接选择完整发布输出、单个 `manifest-v5.json`/`mods-v5.json` 或 ZIP 升级包。升级包只需携带完整终态索引，不必重复携带旧文件；缺少完整索引时发布器会阻止导出。
- 修复旧版升级网关的启动顺序：`legacy/1.9/mods-v4.txt` 与 `legacy/1.6/mods.txt` 现在始终声明可被旧客户端读取的 v4 地址；下载的配置引导 JAR 在升级重启后才把 2.0 客户端切换到最终 `mods-v5.json`，避免 1.9.x 在下载升级组件前因提前看到 v5 地址而终止。
- 加固 Windows 缓存提交：下载完成后写入 `.modsync/cache-v5` 遇到杀毒软件、索引器或另一启动实例短暂占用时，会按递增间隔重试；如果其他进程已经写入同一 SHA-256 的正确缓存则直接复用，不再把一次临时文件移动失败误判为同步失败。

## 1.9.6

### 主要更新

- MCModSync 1.9.x 系列最后一个公开维护版本。
- 修复 NeoForge/Linux 辅助进程的桌面同步窗口。
- 默认最多使用 8 个并行下载线程，并且只重试失败任务。
- 保留已经成功校验的下载，并保存本地 `mods-v4.txt` 清单副本。
- 继续编辑旧清单时生成新的当前时间清单序号。
- 提供中英文界面与日志，以及 Fabric/NeoForge 1.21.1 元数据。

更早的 1.9.x 开发记录保留在 Git 历史中。
